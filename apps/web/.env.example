VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Public production origin used for canonical URLs, social metadata and sitemap.
# Set this to the final HTTPS public site origin before deployment.
VITE_PUBLIC_SITE_URL=
# Set to true for the persistent Cloudflare Web DEV Worker so it cannot be indexed.
VITE_PUBLIC_NO_INDEX=false
# Browser-visible only when restricted by HTTP referrers and Maps API usage.
VITE_GOOGLE_MAPS_BROWSER_KEY=
# Browser-visible image base URL only; never put R2 credentials here.
VITE_R2_PUBLIC_BASE_URL=
